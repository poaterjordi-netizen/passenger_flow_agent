"""HTTP API package for the synthetic passenger-flow mobile client.

Import :mod:`metro_agent.api.app` explicitly to construct the web application.
Keeping package import side-effect free prevents ordinary library users from
creating runtime audit directories.
"""
